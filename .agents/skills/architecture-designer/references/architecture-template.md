# 架构设计模板

## 文档结构

```markdown
# [项目] 架构设计

## 概述
系统架构简述。

## 系统结构

```
src/
├── core/              # 核心业务
├── adapters/          # 适配器
├── ports/             # 端口/接口
└── infrastructure/    # 基础设施
```

## 模块规范

### core/
- **职责**: 业务逻辑
- **接口**: 服务接口
- **依赖**: 无外部依赖

## 组件定义

```typescript
// 服务接口
interface UserService {
  findById(id: string): Promise<User>;
  save(user: User): Promise<void>;
}

// 领域模型
class User {
  id: string;
  name: string;
  email: string;
}
```

## 数据流

[流程图或描述]

## 错误处理策略

| 错误类型 | 处理方式 | 恢复策略 |
|----------|----------|----------|
| 业务错误 | 返回错误码 | 提示用户 |
| 系统错误 | 记录日志 | 降级服务 |

## 决策日志

| 决策 | 选择 | 依据 | 替代方案 | 状态 |
|------|------|------|----------|------|
| 架构风格 | 微服务 | 独立部署 | 单体 | 已确认 |
```

## 设计检查清单

### 模块设计
- [ ] 单一职责
- [ ] 清晰边界
- [ ] 无循环依赖
- [ ] 接口抽象

### 系统设计
- [ ] 可扩展性
- [ ] 可维护性
- [ ] 可测试性
- [ ] 安全性
