# This directory holds the compiled ASN database binary (asn-db.bin.gz)

The binary is built from the BGP.Tools OpenDB CSV via `pnpm build:db`.

Do NOT commit the binary manually — CI rebuilds it weekly.
